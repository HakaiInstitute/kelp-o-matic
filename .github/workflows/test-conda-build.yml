name: '🧬 Test Build'

on:
  pull_request:
    branches: [ main, develop ]

concurrency:
  group: ci-test-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: tayden/hakai-segmentation/.github/workflows/_conda-build.yml@feature/improve-conda-builds
